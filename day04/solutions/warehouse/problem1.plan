(drive fork2 dock aisle-a full half)
(pick-up fork2 pal4 aisle-a)
(drive fork2 aisle-a dock half low)
(put-down fork2 pal4 dock)
(drive fork2 dock aisle-b low empty)
(pick-up fork1 pal1 goods-in)
(drive fork1 goods-in aisle-b full half)
(drive fork1 aisle-b dock half low)
(put-down fork1 pal1 dock)
(recharge fork1 dock low half)
(pick-up fork1 pal5 dock)
(drive fork1 dock aisle-b half low)
(drive fork1 aisle-b goods-in low empty)
(recharge fork1 goods-in empty low)
(put-down fork1 pal5 goods-in)
(recharge fork1 goods-in low half)
(pick-up fork1 pal2 goods-in)
(drive fork1 goods-in aisle-b half low)
(drive fork1 aisle-b dock low empty)
(recharge fork1 dock empty low)
(put-down fork1 pal2 dock)
(recharge fork1 dock low half)
(drive fork1 dock aisle-b half low)
(drive fork1 aisle-b goods-in low empty)
(recharge fork1 goods-in empty low)
(pick-up fork1 pal3 goods-in)
(recharge fork1 goods-in low half)
(drive fork1 goods-in aisle-b half low)
(drive fork1 aisle-b dock low empty)
(put-down fork1 pal3 dock)
; cost = 30 (unit cost)
